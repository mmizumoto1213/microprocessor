This file contains all the .sv files which are used for running my program. The alu.sv, dat_mem.sv, PC.sv, PC_LUT.sv, reg_file.sv, test_bench_new.sv, and top_level.sv are used in modelsim to run the program.

The test0.txt through text9.txt are the test files which we were given.

There is a pdf file called CSE141l Final Minestone.pdf which has an updated version the milestone requirements. It includes a changelog of all the stuff that has been changed since the last milestone.

machine_program1.txt is the machine code that is being used for program1.

The Scripts folder contains 